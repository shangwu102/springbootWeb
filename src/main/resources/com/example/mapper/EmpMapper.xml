<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.EmpMapper">
    <insert id="addEmp">
        insert into emp(username, name, gender, image, job, entrydate, dept_id, create_time, update_time)
        values (#{username},#{name},#{gender},#{image},#{job},#{entrydate},#{deptId},#{createTime},#{updateTime})
    </insert>

    <select id="selEmp" resultType="com.example.pojo.Emp">
        select *
        from my_tli.emp
        <where>
            <if test="name != null">
                name like concat('%', #{name}, '%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and entrydate between #{begin} and #{end}
            </if>
        </where>

        order by update_time desc
        limit #{page}, #{pageSize}
    </select>
    <select id="CountEmp" resultType="integer">
        select count(*) from my_tli.emp
    </select>
    <delete id="delEmp">
        delete
        from emp
        where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selEmpById" resultType="com.example.pojo.Emp">
        select * from emp where id = #{id}
    </select>
    <update id="updEmp">
        update emp
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="job != null">
                job = #{job},
            </if>
            <if test="entrydate != null">
                entrydate = #{entrydate},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id};
    </update>
    <select id="loginEmp" resultType="com.example.pojo.Emp">
        select * from emp where username = #{username} and password = #{password}
    </select>
    <delete id="delById">
        delete from emp where dept_id = #{deptId}
    </delete>

</mapper>